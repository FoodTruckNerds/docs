<footer class="footer">
  <p>
    Built with <a href="https://antora.org" target="_blank" rel="noopener">Antora</a>{{#if antoraVersion}} <span class="antora-version">({{antoraVersion}})</span>{{/if}}.
    Base UI:
    {{#if site.keys.uiBundleName}}
    <a href="{{site.keys.uiBundleUrl}}" target="_blank" rel="noopener">{{site.keys.uiBundleName}}</a>
    {{else}}
    <a href="https://github.com/the-dev-center/antora-dark-theme" target="_blank" rel="noopener">antora-dark-theme</a>
    {{/if}}
    (supplemented by FoodTruckNerdz UI: header, layout rails, navigation, and styling in this repository).
  </p>
  <p>
    The bundled UI&rsquo;s source is
    <a href="https://www.mozilla.org/MPL/2.0/" target="_blank" rel="noopener">MPL-2.0</a>
    licensed; set <code>ui_bundle_name</code> and <code>ui_bundle_url</code> under <code>site.keys</code> in the Antora playbook to match your <code>ui.bundle</code> (exposed as <code>uiBundleName</code> / <code>uiBundleUrl</code> in the UI model).
  </p>
</footer>
